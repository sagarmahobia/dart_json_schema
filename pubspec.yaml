name: dart_json_schema
description: A Dart/Flutter package that provides simple @Field and @JsonSchema annotations for adding metadata to model fields and classes, and generates standard JSON schemas using a command-line tool.
version: 2.1.0
homepage: https://github.com/sagarmahobia/dart_json_schema

environment:
  sdk: '>=2.17.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  analyzer: ^6.0.0
  path: ^1.8.0
  yaml: ^3.1.0
  args: ^2.4.0
  glob: ^2.1.0
  dart_json_schema_annotations: ^2.1.0
  build: ^2.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.21.0
  flutter_lints: ^3.0.0
  build_runner: ^2.3.0

executables:
  dart_json_schema:
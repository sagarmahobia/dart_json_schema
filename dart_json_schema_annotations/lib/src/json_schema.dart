/// A class-level annotation for generating JSON schemas.
///
/// When applied to a class, this annotation indicates that a JSON schema
/// should be generated for all public fields in the class.
class JsonSchema {
  /// Creates a new [JsonSchema] instance.
  const JsonSchema();
}